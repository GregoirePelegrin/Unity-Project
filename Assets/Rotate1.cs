using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotate1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() {
        
    }
/*

    public GameObject articulation1;

    public static GameObject part0;
	public static GameObject part1;

	public int counter = 1;
	public GameObject target = part0;
    public GameObject[] parts = new GameObject[] {part0, part1};*/

    // Update is called once per frame
    void Update() {


/*		if (Input.GetKey(KeyCode.Z)) {
			target.transform.RotateAround(articulation1.transform.position, Vector3.right, -40 * Time.deltaTime);
		}

		if (Input.GetKey(KeyCode.S)) {
			target.transform.RotateAround(articulation1.transform.position, Vector3.right, 40 * Time.deltaTime);
		}
		//articulation2.transform.position = transform.position;*/
    }
}
